let numbUno = parseInt(prompt('Ingresa un número.'));
let numbDos = parseInt(prompt('Ingresa un número.'));
let numbTres = parseInt(prompt('Ingresa un número.'));

if (numbUno>numbDos && numbUno>numbTres) {
    if (numbDos>numbTres && numbDos>numbTres)
        console.log(numbUno,numbDos,numbTres)
} else {
    
}